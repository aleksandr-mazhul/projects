# projects

Umbrella repository. Nested git projects are tracked as submodules so each keeps its own history.

Clone everything:

```bash
git clone --recurse-submodules git@github.com:aleksandr-mazhul/projects.git
```

`health/` and `work/` are placeholders for files copied from other machines.
